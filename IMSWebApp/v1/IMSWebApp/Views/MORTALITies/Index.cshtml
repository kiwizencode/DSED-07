@model IEnumerable<IMSWebApp.Models.DataModel.MORTALITY>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.TEXT)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.OrderBy(x => x.TEXT)) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TEXT)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_PK">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_PK">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_PK">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
