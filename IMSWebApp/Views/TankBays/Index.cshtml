@model IEnumerable<IMSWebApp.Models.TankBay>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.IdCode)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Text)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Text)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPk">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPk">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPk">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
